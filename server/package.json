{
  "name": "gestionale-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply gestionale-ets-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply gestionale-ets-db --remote"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "drizzle-zod": "^0.5.0",
    "hono": "^4.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "drizzle-kit": "^0.21.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.75.0"
  }
}
